# ===========================================
# Client Environment Configuration
# ===========================================
# For LOCAL development (without Docker)
# Copy to .env.local: cp .env.example .env.local
#
# For Docker: use root .env instead (../.env)
# ===========================================

# API Configuration
# NEXT_PUBLIC_* vars are exposed to the browser
NEXT_PUBLIC_API_URL=http://localhost:4000

# Server-side API URL (for SSR)
# In Docker: use container name (http://server:4000)
# Local: same as NEXT_PUBLIC_API_URL
API_URL=

# Site Configuration (used for SEO)
NEXT_PUBLIC_SITE_URL=
NEXT_PUBLIC_SITE_NAME=

# Image Security
# Comma-separated list of allowed external image hosts (e.g., Cloudflare R2)
# Example: your-bucket.account-id.r2.cloudflarestorage.com,cdn.yoursite.com
NEXT_PUBLIC_ALLOWED_IMAGE_HOSTS=

# JWT Secret (server-side only, must match server's JWT_SECRET)
# Used by middleware to verify admin access tokens
JWT_SECRET=

# ===========================================
# Optional: Analytics (uncomment when ready)
# ===========================================
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
# NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=your-verification-code
