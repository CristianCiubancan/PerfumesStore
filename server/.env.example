# ===========================================
# Server Environment Configuration
# ===========================================
# For LOCAL development (without Docker)
# Copy to .env: cp .env.example .env
#
# For Docker: use root .env instead (../env)
# ===========================================

# Environment
NODE_ENV=

# Server
PORT=

# Database
# Local: localhost:5432 (or 5433 if using Docker postgres)
DATABASE_URL=

# JWT Authentication (REQUIRED - min 32 characters each)
# Generate with: openssl rand -base64 32
JWT_SECRET=
JWT_REFRESH_SECRET=

# Admin Seed Password (for initial admin user)
# If not set, a random password will be generated during seeding
ADMIN_SEED_PASSWORD=

# URLs
CLIENT_URL=
BACKEND_URL=

# Image Security
# Comma-separated list of allowed external image hosts (e.g., Cloudflare R2)
# Example: your-bucket.account-id.r2.cloudflarestorage.com,cdn.yoursite.com
ALLOWED_IMAGE_HOSTS=
